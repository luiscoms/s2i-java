#!/bin/bash -e
#
# S2I run script for the 's2i-java8-gradle' image.
# The run script executes the server that runs your application.
#
# For more information see the documentation:
#	https://github.com/openshift/source-to-image/blob/master/docs/builder_image.md
#

ARTIFACT_DIR=${ARTIFACT_DIR:-build}
echo "---> Starting Spring Boot application"
echo "--> # APP_FILE = $APP_FILE"
echo "--> # JAVA_OPTS = $JAVA_OPTS"
echo "--> # APP_OPTS = $APP_OPTS"
APP_FILE="${APP_FILE:-build/app.jar}"
if [[ -f "$APP_FILE" ]]; then
  echo "---> Running application from jar ($APP_FILE) ..."
  java $JAVA_OPTS -jar $APP_FILE $APP_OPTS
else
  echo "---> Running application from jar ($(find $ARTIFACT_DIR -name *.jar)) ..."
  java $JAVA_OPTS -jar `find $ARTIFACT_DIR -name *.jar` $APP_OPTS
fi
